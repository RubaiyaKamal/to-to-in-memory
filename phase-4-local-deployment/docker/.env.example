# Docker Compose Environment Variables - Example Template
# Copy this file to .env and fill in your actual values
#
# cp .env.example .env
#
# IMPORTANT: Never commit .env to git (already in .gitignore)

# =============================================================================
# OpenAI API Configuration (REQUIRED)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# =============================================================================
# Authentication Configuration (REQUIRED for production)
# =============================================================================
# Generate a secure random string (minimum 32 characters)
# Example generation: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long-change-this

# Base URL for authentication callbacks
# For local development with Docker Compose:
BETTER_AUTH_URL=http://localhost:8001

# =============================================================================
# Frontend Configuration (OPTIONAL)
# =============================================================================
# Backend API URL for Vite build
# Default: http://localhost:8001 (access from host machine)
# Alternative: http://backend:8001 (access from within Docker network)
VITE_API_URL=http://localhost:8001

# =============================================================================
# Database Configuration (OPTIONAL - Auto-configured for local development)
# =============================================================================
# For Docker Compose, SQLite is automatically configured in docker-compose.yml
# DATABASE_URL is set to: sqlite:////app/data/todo.db
#
# If you want to use external PostgreSQL instead:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
#
# Example with Neon:
# DATABASE_URL=postgresql://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require

# =============================================================================
# Minikube Configuration (OPTIONAL - For Task 2.x)
# =============================================================================
# These settings are for Minikube setup, not used by Docker Compose
MINIKUBE_CPUS=4
MINIKUBE_MEMORY=8192
MINIKUBE_DISK_SIZE=20g
